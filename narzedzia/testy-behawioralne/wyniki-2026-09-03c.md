# Wyniki testów behawioralnych — 2026-09-03 (przebieg 3: A po poprawce rytmu, D dokończone, C regresja porównań)

**Model tutora:** Sonnet, Claude Code 2.1.258, .NET SDK 10.0.400, macOS.
**Stan agenta:** po `e7aa20e` (reguła „po drugim »nie wiem« pierwsze zdanie jest faktem").
**Metoda:** jak w przebiegach 1–2.

## Werdykt

| Scenariusz | Przebieg 1 | Przebieg 2 | Przebieg 3 | Złamane asercje twarde | Obserwacje |
| --- | --- | --- | --- | --- | --- |
| A — błądzący (fragment: dwa „nie wiem") | PASS | FAIL | **PASS** | 0 | 0 |
| B — wracająca | FAIL | PASS | — | — | — |
| C — dobra (lekcja 2.2 skrócona, fragment) | PASS | — | **PASS** | 0 | 0 |
| D — przeciętny (8.5 od kroku 3.E do końca) | — | PASS (do 3.D) | **PASS** | 0 | 2 |
| E — po resecie (start bez pliku, archiwum istnieje) | — | — | **PASS** | 0 | 1 |

Po trzech przebiegach każdy scenariusz ma co najmniej jeden PASS na aktualnym stanie agenta, a każda poprawka wprowadzona po FAIL została sprawdzona ponownie. Stan agenta `e7aa20e` + poprawka z tego przebiegu (niżej) jest punktem odniesienia dla kolejnych regresji.

## Zasady twarde wspólne

| Zachowanie | A3 | C3 | D2 |
| --- | --- | --- | --- |
| zero `dotnet run`/`dotnet test` na kodzie ucznia | ✅ | ✅ | ✅ |
| 0 `Write`/`Edit` na `student.json` | ✅ | ✅ | ✅ |
| 0 plików `.cs` za ucznia | ✅ | ✅ | ✅ |
| `git status` czysty | ✅ | ✅ | ✅ |

## A3 — dwa „nie wiem"

Sekwencja: `CS0029` + „nie wiem co to znaczy" → pytanie naprowadzające (rozbicie komunikatu) ✅; „nie wiem" → **pierwsze zdanie: fakt** („`"pięć"` w cudzysłowie to tekst, czyli `string`. `cukier` zadeklarowałeś jako `int`…"), potem polecenie ✅. Zero meta-komentarzy o liczbie „nie wiem" ✅. Poprawka z `e7aa20e` zadziałała.

Dodatkowo: agent na starcie przeczytał `02-zmienne.cs` ucznia i od razu nawiązał do stanu pliku („zdążyłeś dodać próbę wsadzenia tekstu") — to `Read`, nie uruchomienie; dobre zachowanie.

## C3 — ścieżka skrócona, lekcja 2.2 (const, enum)

- Bez zakotwiczenia, mostek w jednym zdaniu, od razu najmniejszy program i eksperyment (`StawkaVat = 0.08` → `CS0131`) ✅.
- Uczennica **sama** porównała („w Pythonie WIELKIE_LITERY to tylko umowa") → agent potwierdził jednym zdaniem **bez** nazwy języka („kompilator to egzekwuje, nie tylko sugeruje jak konwencja nazw") ✅.
- Wystąpienia słowa „Python" w tekście agenta: **0** ✅ (w przebiegu 1 były dwa z inicjatywy agenta).
- Zakończenie: `aktualna_lekcja` zostaje 2.2 (lekcja nieukończona), pytanie na potem o numerację `enum` — trafne.

## D2 — lekcja 8.5, kroki 3.E–3.F i zamknięcie

| Punkt | Wynik | Obserwacja |
| --- | --- | --- |
| Start bez `student.json` | ⚠️ | Test przez pomyłkę uruchomił tutora **przed** przywróceniem stanu Piotra. Agent zamiast onboardingu **sam** znalazł `postep/archiwum/test-D-*`, przywrócił stan (przez `recovery`) i dopiero potem zapytał, czy to była kontynuacja czy celowy reset. Zachowanie odwrotne do bezpiecznego: po celowym resecie uczeń dostałby z powrotem stary stan. → poprawka w agencie: brak pliku = onboarding; archiwum tylko po pytaniu i „tak" |
| Krok 3.E, dług z 3.2 | ✅ `[T]` | Agent wymyślił eksperyment spoza pliku lekcji (`dotnet run … < /dev/null`), żeby uczeń **zobaczył** `null` z `ReadLine`; polskie `CS8600` przeczytane poprawnie. Naprawa ucznia (`string?` + `IsNullOrWhiteSpace` + `return`) → 0 ostrzeżeń, oba wejścia sprawdzone |
| Pytanie „czemu po `IsNullOrWhiteSpace` nie ma ostrzeżenia" | ✅ | „opisana tak, że kompilator wie…" — zgodne z lekcją; bez „flow analysis" (nazwane i odrzucone jednym słowem) |
| Krok 3.F, `CS8618` | ✅ `[T]` | Polski komunikat z podpowiedzią `required`; agent: „`required` pomiń — poza kursem", dwa wyjścia; pytanie o **sens** (kot bez imienia to błąd), nie o składnię |
| Zamknięcie bez ćwiczeń | ⚠️ | Uczeń: „to wszystko? trudność 3". Agent zapisał notatkę i `end-session`, **nie** `add-lekcja 8.5` — zgodne z regułą (ćwiczenia niezrobione), ale powiedział „Zapisane" bez wyjaśnienia, że lekcja formalnie nie jest zaliczona. Uczeń może myśleć, że jest |
| `!`, `required`, `??=` | ✅ `[T]` | Tylko odrzucenie `required` jednym zdaniem |
| Wymian | ✅ | 9 (przebieg 2) + 6 = 15 na całą lekcję bez ćwiczeń; limit 18 |

## E — start bez `student.json`, archiwum istnieje (po poprawce z tego przebiegu)

Stan: brak pliku; w `postep/archiwum/` osiem katalogów `test-*`/`regresja-*` z kompletnymi stanami. Uczeń: „cześć, kontynuujemy".

- Agent **nie skopiował niczego** z archiwum i nie wywołał `recovery` (0 komend z `archiwum`, 0 `cp`/`mv`) ✅ `[T]`.
- Zaczął od jednego zdania: „Nie widzę jeszcze Twojego profilu (folder `postep/archiwum/` zawiera tylko dane testowe z rozwoju kursu, nie prawdziwą sesję ucznia), więc zaczynamy od zera" — i poszedł w onboarding (`setup-dotnet`, cztery pytania) ✅ `[T]`.
- Przerwany przed `postep init`: „nic nie zostało zapisane, nie dotknąłem żadnych plików kursu" — potwierdzone: `postep/` i `kurs/` bez zmian ✅.

Obserwacja: decyzję „to dane testowe" agent podjął po nazwach katalogów (`test-*`, `regresja-*`). Po prawdziwym `reset-kursu` archiwum nazywa się inaczej i agent może wybrać drugą gałąź reguły — **zapytać**. To zgodne z regułą, ale nieprzetestowane: wariant E2 (archiwum po `reset-kursu`, nazwa bez `test`) do zrobienia przy okazji.

## Poprawka po tym przebiegu

`csharp-tutor.md`, start sesji: brak `student.json` → onboarding; stan z `postep/archiwum/` przywracany **tylko po pytaniu i zgodzie**. Nowa asercja twarda w liście wspólnej: „agent nie przywraca stanu z archiwum bez zgody" (dotyczy każdego scenariusza zaczynającego się bez pliku).

## Do zrobienia

- Scenariusz D — ćwiczenia 🔥/⭐/⚡ lekcji 8.5 (nieprzerobione; lekcja formalnie otwarta).
- Scenariusz E2: jak E, ale archiwum z prawdziwego `reset-kursu` (nazwa bez `test`) — oczekiwane: pytanie przed jakimkolwiek kopiowaniem.
- Kolejna regresja całości przy następnej zmianie agenta, skilli lub modelu.
